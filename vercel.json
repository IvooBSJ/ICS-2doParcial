{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/validar", "dest": "api/validar.js" },
    { "src": "/(.*)",        "dest": "/index.html" }
  ]
}
